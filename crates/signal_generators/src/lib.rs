//! Explicit deterministic signal sources for tests, demonstrations, and authored scenarios.
//!
//! These generators are selected through configuration; they are never implicit fallbacks for an
//! unavailable host device or capture source.

pub mod synthetic_capture_source;
pub mod synthetic_uart_source;
